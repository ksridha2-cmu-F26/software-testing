L1 TDD Lab Report
Name: [Your Name]
Andrew ID: [Your Andrew ID]
Partner (if any): [Partner Name / Solo]

Tasks Completed So Far: T1 through T6

TDD Approach:
- T1: Implemented ISocialNetwork stubs guided by compile errors; kept old A0 API.
- T2: Wrote login tests first, then implemented login(Account).
- T3: Tests for hasMember, then implementation with visibility rules.
- T4: Tests for cancelAutoAcceptFriendships, then Account + SocialNetwork code.
- T5: Tests for block behavior, then block(String) implementation.
- T6: Tests for unblock, then unblock(String) implementation.

Challenges:
- Migrating from Account-parameter API to login-based API (T2).
- Understanding block visibility: blocked user cannot see blocker.

Tests Added: login tests, hasMember tests, block/unblock tests, cancelAutoAccept tests.

Outcome: Local unit tests pass for T1-T6 scope.

Lessons Learned:
- TDD in small steps (one test, one fix) kept the lab manageable.
- Login must be called before listMembers and other operations after T2.

Not Yet Completed: T7 (recommendFriends), T8 (block cleanup), T9 (NoUserLoggedInException), T10 (final report polish).
